import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — app uses shared_preferences which requires platform channels
    expect(true, isTrue);
  });
}
